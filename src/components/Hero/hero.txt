// jsx


import React, { useEffect, useRef, useState } from 'react';
import { ChevronDown, Sparkles, Zap, Globe, ArrowRight, Play, Star } from 'lucide-react';
import { Brain } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import './Hero.css';

const Hero = () => {
  const heroRef = useRef(null);
  const particlesRef = useRef(null);
  const [mousePosition, setMousePosition] = useState({ x: 0, y: 0 });
  const [scrollProgress, setScrollProgress] = useState(0);
  const navigate = useNavigate();

  // Handle mouse movement for interactive background effects
  useEffect(() => {
    const handleMouseMove = (e) => {
      setMousePosition({
        x: (e.clientX / window.innerWidth) * 2 - 1,
        y: (e.clientY / window.innerHeight) * 2 - 1,
      });
    };

    window.addEventListener('mousemove', handleMouseMove);
    return () => window.removeEventListener('mousemove', handleMouseMove);
  }, []);

  // Handle scroll with requestAnimationFrame for smooth performance
  useEffect(() => {
    let animationFrameId;
    const handleScroll = () => {
      if (animationFrameId) cancelAnimationFrame(animationFrameId);
      animationFrameId = requestAnimationFrame(() => {
        if (!heroRef.current) return;
        const scrollTop = window.scrollY;
        const windowHeight = window.innerHeight;
        const maxScroll = windowHeight * 0.4;
        const progress = Math.min(scrollTop / maxScroll, 1);
        setScrollProgress(progress);
      });
    };

    window.addEventListener('scroll', handleScroll, { passive: true });
    return () => {
      window.removeEventListener('scroll', handleScroll);
      if (animationFrameId) cancelAnimationFrame(animationFrameId);
    };
  }, []);

  // Create and animate particle effects
  useEffect(() => {
    const createParticle = () => {
      if (!particlesRef.current) return;

      const particle = document.createElement('div');
      particle.className = 'ph2-hero-particle';
      particle.style.left = `${Math.random() * 100}%`;
      particle.style.animationDuration = `${Math.random() * 4 + 3}s`;
      particle.style.opacity = Math.random() * 0.8;

      const colors = ['#00FFFF', '#FF00FF', '#0066FF', '#FF6600', '#8A2BE2'];
      particle.style.boxShadow = `0 0 10px ${colors[Math.floor(Math.random() * colors.length)]}`;

      particlesRef.current.appendChild(particle);

      setTimeout(() => {
        if (particle.parentNode) {
          particle.parentNode.removeChild(particle);
        }
      }, 7000);
    };

    const interval = setInterval(createParticle, 150);
    return () => clearInterval(interval);
  }, []);

  // Handle card click navigation
  const handleCardClick = (path) => {
    navigate(path);
  };

  return (
    <section
      className="ph2-hero"
      ref={heroRef}
      style={{
        height: `calc(100vh - ${scrollProgress * 50}vh)`,
        opacity: 1 - scrollProgress * 0.25,
        borderBottomLeftRadius: `${scrollProgress * 100}px`,
        borderBottomRightRadius: `${scrollProgress * 100}px`,
        transform: `scale(${1 - scrollProgress * 0.2})`,
      }}
    >
      <div className="ph2-hero-particles" ref={particlesRef}></div>

      <div className="ph2-hero-background">
        <div
          className="ph2-gradient-sphere ph2-sphere-1"
          style={{
            transform: `translate(${mousePosition.x * 30}px, ${mousePosition.y * 30}px) scale(${1 - scrollProgress * 0.25})`,
            opacity: 1 - scrollProgress * 0.35,
          }}
        ></div>
        <div
          className="ph2-gradient-sphere ph2-sphere-2"
          style={{
            transform: `translate(${mousePosition.x * -20}px, ${mousePosition.y * -20}px) scale(${1 - scrollProgress * 0.25})`,
            opacity: 1 - scrollProgress * 0.35,
          }}
        ></div>
        <div
          className="ph2-gradient-sphere ph2-sphere-3"
          style={{
            transform: `translate(${mousePosition.x * 40}px, ${mousePosition.y * -25}px) scale(${1 - scrollProgress * 0.25})`,
            opacity: 1 - scrollProgress * 0.35,
          }}
        ></div>
      </div>

      <div
        className="ph2-hero-content"
        style={{
          transform: `scale(${1 - scrollProgress * 0.2})`,
          opacity: 1 - scrollProgress * 0.35,
        }}
      >
        <div className="ph2-hero-text">
          <div className="ph2-hero-badge">
            <Star className="ph2-badge-icon" />
            <span>Premium 3D Innovation</span>
          </div>

          <h1 className="ph2-hero-title">
            <span className="ph2-title-line ph2-animate-slide-in">Welcome to the Future</span>
            {/* <img
              src="/src/public/images/Logo.png"
              alt="Pixel Horse Logo"
              className="ph2-title-main ph2-animate-scale-in ph-logo-image"
            /> */}
            <img
              src="/public/images/Logo.png"
              alt="Pixel Horse Logo"
              className="ph2-title-main ph2-animate-scale-in ph-logo-image"
            />
            <span className="ph2-title-subtitle ph2-animate-slide-in-right">3D Anamorphic Experiences</span>
          </h1>

          <p className="ph2-hero-description ph2-animate-fade-in">
            Transform your digital presence with cutting-edge 3D anamorphic advertising, revolutionary AI systems, and
            innovative solutions that push the boundaries of technology and creativity.
          </p>

          <div className="ph2-hero-stats ph2-animate-fade-in">
            <div className="ph2-stat-item">
              <div className="ph2-stat-number">50+</div>
              <div className="ph2-hero-stat-label">Projects Delivered</div>
            </div>
            <div className="ph2-stat-item">
              <div className="ph2-stat-number">98%</div>
              <div className="ph2-hero-stat-label">Client Satisfaction</div>
            </div>
            <div className="ph2-stat-item">
              <div className="ph2-stat-number">24/7</div>
              <div className="ph2-hero-stat-label">Support Available</div>
            </div>
          </div>

          <div className="ph2-hero-buttons ph2-animate-fade-in">
            <button className="ph2-cta-button ph2-cta-button-primary">
              <Zap className="ph2-button-icon" />
              <span>Explore Our Services</span>
              <ArrowRight className="ph2-button-arrow" />
            </button>
            <button className="ph2-cta-button ph2-cta-button-secondary">
              <Play className="ph2-button-icon" />
              <span>Watch Demo Reel</span>
            </button>
          </div>
        </div>

        <div className="ph2-hero-visual">
          <div className="ph2-floating-elements">
            <div
              className="ph2-floating-card ph2-card-1 ph2-animate-float"
              onClick={() => handleCardClick('/3d-advertising')}
            >
              <div className="ph2-card-inner">
                <Sparkles className="ph2-card-icon" />
                <span className="ph2-card-text-Advertising">3D Advertising</span>
                <div className="ph2-card-glow"></div>
              </div>
            </div>
            <div
              className="ph2-floating-card ph2-card-2 ph2-animate-float"
              onClick={() => handleCardClick('/ai-systems')}
            >
              <div className="ph2-card-inner">
                <Brain className="ph2-card-icon" />
                <span className="ph2-card-text-Systems">AI Systems</span>
                <div className="ph2-card-glow"></div>
              </div>
            </div>
            <div
              className="ph2-floating-card ph2-card-3 ph2-animate-float"
              onClick={() => handleCardClick('/it-solutions')}
            >
              <div className="ph2-card-inner">
                <Globe className="ph2-card-icon" />
                <span className="ph2-card-text-Solutions">IT Solutions</span>
                <div className="ph2-card-glow"></div>
              </div>
            </div>
          </div>

          <div
            className="ph2-central-display"
            style={{
              transform: `scale(${1 - scrollProgress * 0.3})`,
              opacity: 1 - scrollProgress * 0.5,
            }}
          >
            <div className="ph2-display-frame">
              <div className="ph2-hologram-rings">
                <div
                  className="ph2-holo-ring ph2-ring-1"
                  style={{
                    transform: `scale(${1 - scrollProgress * 0.3})`,
                  }}
                ></div>
                <div
                  className="ph2-holo-ring ph2-ring-2"
                  style={{
                    transform: `scale(${1 - scrollProgress * 0.3})`,
                  }}
                ></div>
                <div
                  className="ph2-holo-ring ph2-ring-3"
                  style={{
                    transform: `scale(${1 - scrollProgress * 0.3})`,
                  }}
                ></div>
                <div
                  className="ph2-holo-ring ph2-ring-4"
                  style={{
                    transform: `scale(${1 - scrollProgress * 0.3})`,
                  }}
                ></div>
              </div>
              <div className="ph2-display-core">
                <div className="ph2-core-orb"></div>
                <div className="ph2-core-pulse"></div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div
        className="ph2-scroll-indicator ph2-animate-bounce"
        style={{
          opacity: 1 - scrollProgress * 1.5,
        }}
      >
        <div className="ph2-scroll-line"></div>
        <ChevronDown className="ph2-scroll-icon" />
        <span className="ph2-scroll-text">Discover More</span>
      </div>
    </section>
  );
};

export default Hero;



// css


/* Main hero section styling with unique layout, perspective, and border */
.ph2-hero {
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
  background: rgb(0, 0, 0);
  perspective: 2000px;
  transition: height 0.4s ease-out, opacity 0.4s ease-out, transform 0.4s ease-out, border-radius 0.4s ease-out;
  will-change: height, opacity, transform, border-radius;
  border: 2px solid #00FFFF;
  box-shadow: 0 0 20px rgba(0, 255, 255, 0.5), inset 0 0 10px rgba(0, 255, 255, 0.3);
  animation: ph2-borderGlow 4s ease-in-out infinite alternate;
}

/* Border glow animation */
@keyframes ph2-borderGlow {
  0% {
    box-shadow: 0 0 20px rgba(0, 255, 255, 0.5), inset 0 0 10px rgba(0, 255, 255, 0.3);
    border-color: #00FFFF;
  }
  100% {
    box-shadow: 0 0 30px rgba(255, 0, 255, 0.5), inset 0 0 15px rgba(255, 0, 255, 0.3);
    border-color: #FF00FF;
  }
}

/* Ensure smooth scrolling behavior for the page */
html {
  scroll-behavior: smooth;
}

/* Container for animated particle effects */
.ph2-hero-particles {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 1;
}

/* Individual particle styling with animation */
.ph2-hero-particle {
  position: absolute;
  width: 4px;
  height: 4px;
  background: radial-gradient(circle, currentColor, transparent);
  border-radius: 50%;
  animation: ph2-particleFloat 7s linear infinite;
  will-change: transform, opacity;
}

@keyframes ph2-particleFloat {
  0% {
    transform: translateY(100vh) scale(0) rotate(0deg);
    opacity: 0;
  }
  10% {
    opacity: 1;
  }
  90% {
    opacity: 1;
  }
  100% {
    transform: translateY(-10vh) scale(1.5) rotate(360deg);
    opacity: 0;
  }
}

/* Background layer for gradient spheres */
.ph2-hero-background {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 0;
}

/* Base styling for gradient spheres with unique positioning */
.ph2-gradient-sphere {
  position: absolute;
  border-radius: 50%;
  filter: blur(50px);
  animation: ph2-sphereFloat 12s ease-in-out infinite;
  transition: transform 0.4s ease-out, opacity 0.4s ease-out;
  z-index: 0;
  will-change: transform, opacity;
}

/* Individual sphere styles with adjusted positioning to avoid overlap */
.ph2-sphere-1 {
  width: 400px;
  height: 400px;
  top: 10%;
  left: 5%;
  background: radial-gradient(circle, #00FFFF 0%, rgba(255, 0, 255, 0.3) 40%, transparent 70%);
  animation-delay: 0s;
}

.ph2-sphere-2 {
  width: 350px;
  height: 350px;
  top: 50%;
  right: 5%;
  background: radial-gradient(circle, #FF00FF 0%, rgba(255, 0, 255, 0.3) 40%, transparent 80%);
  animation-delay: 4s;
}

.ph2-sphere-3 {
  width: 250px;
  height: 250px;
  bottom: 10%;
  left: 70%;
  background: radial-gradient(circle, #FF6600 0%, rgba(255, 102, 0, 0.3) 40%, transparent 80%);
  animation-delay: 8s;
}

@keyframes ph2-sphereFloat {
  0%, 100% { transform: translate(0, 0) scale(1); }
  33% { transform: translate(30px, -20px) scale(1.1); }
  66% { transform: translate(-20px, 20px) scale(0.9); }
}

/* Content grid for text and visual elements */
.ph2-hero-content {
  max-width: 1600px;
  width: 100%;
  padding: 0 2rem;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 5rem;
  align-items: center;
  z-index: 2;
  position: relative;
  transition: transform 0.4s ease-out, opacity 0.4s ease-out;
  will-change: transform, opacity;
}

/* Text section with 3D perspective */
.ph2-hero-text {
  perspective: 1000px;
  transform-style: preserve-3d;
}

/* Badge styling for the innovation label */
.ph2-hero-badge {
  display: inline-flex;
  align-items: center;
  margin-top: 40px;
  gap: 0.5rem;
  padding: 0.5rem 1rem;
  background: rgba(0, 255, 255, 0.1);
  border: 1px solid rgba(0, 255, 255, 0.3);
  border-radius: 25px;
  color: #00FFFF;
  font-size: 0.9rem;
  font-weight: 500;
  margin-bottom: 2rem;
  backdrop-filter: blur(20px);
  animation: ph2-badgeGlow 3s ease-in-out infinite alternate;
}

.ph2-badge-icon {
  width: 1rem;
  height: 1rem;
  animation: ph2-iconSpin 4s linear infinite;
}

@keyframes ph2-badgeGlow {
  0% { box-shadow: 0 0 20px rgba(0, 255, 255, 0.3); }
  100% { box-shadow: 0 0 30px rgba(0, 255, 255, 0.6); }
}

@keyframes ph2-iconSpin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* Title styling with animated lines */
.ph2-hero-title {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  margin-bottom: 2rem;
}

.ph2-title-line {
  font-size: 1.6rem;
  color: #00FFFF;
  font-weight: 300;
  opacity: 0;
  transform: translateX(-100px);
}

.ph2-title-main {
  position: relative;
  text-shadow: 0 0 40px rgba(0, 255, 255, 0.5);
  transform: scale(0);
  opacity: 0;
}

.ph2-logo-image {
  height: 5rem; /* Matches original font-size */
  width: auto;
  max-width: 100%;
  display: block;
  animation: ph2-rainbowGradient 6s ease infinite;
  background: linear-gradient(45deg, #00FFFF, #0066FF, #FF00FF, #FF6600, #8A2BE2);
  background-size: 400% 400%;
  -webkit-mask-image: linear-gradient(45deg, rgba(0, 255, 255, 0.8), rgba(255, 0, 255, 0.8)); /* Gradient overlay effect */
  mask-image: linear-gradient(45deg, rgba(0, 255, 255, 0.8), rgba(255, 0, 255, 0.8));
}

.ph2-title-subtitle {
  font-size: 1.4rem;
  color: #8A2BE2;
  font-weight: 400;
  opacity: 0;
  transform: translateX(100px);
}

@keyframes ph2-rainbowGradient {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.ph2-animate-slide-in {
  animation: ph2-slideInLeft 1s ease 0.5s forwards;
}

.ph2-animate-scale-in {
  animation: ph2-scaleIn 1.2s cubic-bezier(0.23, 1, 0.32, 1) 1s forwards;
}

.ph2-animate-slide-in-right {
  animation: ph2-slideInRight 1s ease 1.5s forwards;
}

.ph2-animate-fade-in {
  animation: ph2-fadeInUp 1s ease 2s forwards;
}

@keyframes ph2-slideInLeft {
  to {
    opacity: 1;
    transform: translateX(0) rotateY(0deg);
  }
}

@keyframes ph2-slideInRight {
  to {
    opacity: 1;
    transform: translateX(0) rotateY(0deg);
  }
}

@keyframes ph2-scaleIn {
  to {
    opacity: 1;
    transform: scale(1) rotateX(0deg);
  }
}

@keyframes ph2-fadeInUp {
  to {
    opacity: 1;
    transform: translateY(0) rotateX(0deg);
  }
}

/* Description text with animation */
.ph2-hero-description {
  font-size: 1.3rem;
  line-height: 1.8;
  color: rgb(255, 255, 255);
  margin-bottom: 3rem;
  opacity: 0;
  transform: translateY(50px);
}

/* Stats display with animated entries */
.ph2-hero-stats {
  display: flex;
  gap: 2rem;
  margin-bottom: 3rem;
  opacity: 0;
  transform: translateY(30px);
}

.ph2-stat-item {
  text-align: center;
  perspective: 1000px;
}

.ph2-stat-number {
  font-size: 2.5rem;
  font-weight: 900;
  background: linear-gradient(45deg, #00FFFF, #FF00FF);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  margin-bottom: 0.5rem;
  animation: ph2-statPulse 3s ease-in-out infinite;
}

.ph2-hero-stat-label {
  font-size: 0.9rem;
  color: rgb(255, 255, 255);
  text-transform: uppercase;
  letter-spacing: 1px;
}

@keyframes ph2-statPulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05) rotateY(5deg); }
}

/* Buttons with hover effects */
.ph2-hero-buttons {
  display: flex;
  gap: 1.5rem;
  opacity: 0;
  transform: translateY(30px);
}

.ph2-cta-button {
  display: flex;
  align-items: center;
  gap: 0.8rem;
  padding: 1.2rem 2rem;
  border: none;
  border-radius: 50px;
  font-size: 1.1rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1);
  transform-style: preserve-3d;
  position: relative;
  overflow: hidden;
}

.ph2-cta-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
  transition: left 0.6s ease;
}

.ph2-cta-button:hover::before {
  left: 100%;
}

.ph2-cta-button-primary {
  background: linear-gradient(45deg, #00FFFF, #0066FF, #FF00FF);
  background-size: 200% 200%;
  color: white;
  box-shadow: 0 15px 40px rgba(0, 255, 255, 0.4);
  animation: ph2-buttonGradient 3s ease infinite;
}

.ph2-cta-button-primary:hover {
  transform: translateY(-8px) rotateX(20deg) scale(1.05);
  box-shadow: 0 25px 50px rgba(0, 255, 255, 0.6);
}

.ph2-cta-button-secondary {
  background-color: #ff65ff;
  color: white;
  border: 2px solid #FF00FF;
  box-shadow: 0 15px 40px rgba(255, 0, 255, 0.3);
}

.ph2-cta-button-secondary:hover {
  background: linear-gradient(45deg, #FF00FF, #8A2BE2);
  transform: translateY(-8px) rotateX(20deg) scale(1.05);
  box-shadow: 0 25px 50px rgba(255, 0, 255, 0.6);
}

@keyframes ph2-buttonGradient {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.ph2-button-icon {
  width: 1.2rem;
  height: 1.2rem;
  transition: transform 0.3s ease;
}

.ph2-button-arrow {
  width: 1rem;
  height: 1rem;
  transition: transform 0.3s ease;
}

.ph2-cta-button:hover .ph2-button-icon,
.ph2-cta-button:hover .ph2-button-arrow {
  transform: rotateY(180deg) scale(1.2);
}

/* Visual section with floating cards and central display */
.ph2-hero-visual {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 700px;
  perspective: 2000px;
  transform-style: preserve-3d;
  z-index: 2;
  will-change: transform;
}

/* Container for floating cards */
.ph2-floating-elements {
  position: absolute;
  width: 100%;
  height: 100%;
  z-index: 1;
}

/* Individual floating card styling */
.ph2-floating-card {
  position: absolute;
  perspective: 1000px;
  transform-style: preserve-3d;
  cursor: pointer;
}

/* Card inner styling with transparent background and visible text */
.ph2-card-inner {
  display: flex;
  align-items: center;
  flex-direction: column;
  gap: 0.8rem;
  padding: 1.5rem;
  background: transparent;
  border: 2px solid var(--ph2-card-color);
  border-radius: 25px;
  color: var(--ph2-card-color);
  font-weight: 600;
  transition: all 0.4s ease;
  position: relative;
  overflow: hidden;
  will-change: transform;
}

.ph2-card-text-Advertising,
.ph2-card-text-Systems,
.ph2-card-text-Solutions {
  font-size: 1rem;
  text-align: center;
}

.ph2-card-glow {
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle, var(--ph2-card-color) 0%, transparent 70%);
  opacity: 0;
  transition: opacity 0.3s ease;
  animation: ph2-glowRotate 6s linear infinite;
  will-change: transform, opacity;
}

@keyframes ph2-glowRotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.ph2-floating-card:hover .ph2-card-glow {
  opacity: 0.2;
}

.ph2-card-1 {
  top: 15%;
  right: 20%;
  --ph2-card-color: #00FFFF;
  animation-delay: 0s;
}

.ph2-card-2 {
  top: 55%;
  right: 5%;
  --ph2-card-color: #FF00FF;
  animation-delay: 2s;
}

.ph2-card-3 {
  bottom: 15%;
  right: 25%;
  --ph2-card-color: #FF6600;
  animation-delay: 4s;
}

.ph2-animate-float {
  animation: ph2-cardFloat 6s ease-in-out infinite;
}

@keyframes ph2-cardFloat {
  0%, 100% {
    transform: translateY(0) rotateX(0deg) rotateY(0deg) rotateZ(0deg);
  }
  25% {
    transform: translateY(-25px) rotateX(10deg) rotateY(5deg) rotateZ(2deg);
  }
  50% {
    transform: translateY(-10px) rotateX(5deg) rotateY(-5deg) rotateZ(-2deg);
  }
  75% {
    transform: translateY(-20px) rotateX(-5deg) rotateY(10deg) rotateZ(1deg);
  }
}

.ph2-card-inner:hover {
  transform: rotateY(15deg) rotateX(10deg) scale(1.1);
  border-color: var(--ph2-card-color);
  box-shadow: 0 20px 50px rgba(var(--ph2-card-color), 0.4);
}

.ph2-card-icon {
  width: 2rem;
  height: 2rem;
  color: var(--ph2-card-color);
  animation: ph2-iconFloat 4s ease-in-out infinite;
}

@keyframes ph2-iconFloat {
  0%, 100% { transform: translateY(0) rotateZ(0deg); }
  50% { transform: translateY(-10px) rotateZ(180deg); }
}

/* Central display with hologram rings */
.ph2-central-display {
  position: relative;
  width: 300px;
  height: 300px;
  animation: ph2-displayFloat 8s ease-in-out infinite;
  z-index: 2;
  will-change: transform, opacity;
  transition: transform 0.4s ease-out, opacity 0.4s ease-out;
}

@keyframes ph2-displayFloat {
  0%, 100% { transform: translateY(0) rotateY(0deg); }
  50% { transform: translateY(-30px) rotateY(180deg); }
}

.ph2-display-frame {
  position: relative;
  width: 100%;
  height: 100%;
  border-radius: 50%;
  background: rgba(0, 255, 255, 0.03);
  border: 2px solid rgba(0, 255, 255, 0.3);
  animation: ph2-frameRotate 20s linear infinite;
  z-index: 3;
}

@keyframes ph2-frameRotate {
  from { transform: rotateZ(0deg); }
  to { transform: rotateZ(360deg); }
}

.ph2-hologram-rings {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 2;
}

.ph2-holo-ring {
  position: absolute;
  border-radius: 50%;
  border: 1px solid;
  animation: ph2-ringRotate 15s linear infinite;
  z-index: 1;
  will-change: transform;
}

.ph2-ring-1 {
  width: 100%;
  height: 100%;
  border-color: #00FFFF;
  opacity: 0.8;
  animation-duration: 15s;
}

.ph2-ring-2 {
  width: 80%;
  height: 80%;
  top: 10%;
  left: 10%;
  border-color: #FF00FF;
  opacity: 0.6;
  animation-duration: 12s;
  animation-direction: reverse;
}

.ph2-ring-3 {
  width: 60%;
  height: 60%;
  top: 20%;
  left: 20%;
  border-color: #0066FF;
  opacity: 0.5;
  animation-duration: 10s;
}

.ph2-ring-4 {
  width: 40%;
  height: 40%;
  top: 30%;
  left: 30%;
  border-color: #FF6600;
  opacity: 0.4;
  animation-duration: 8s;
  animation-direction: reverse;
}

@keyframes ph2-ringRotate {
  from { transform: rotateX(60deg) rotateY(0deg) translateZ(0); }
  to { transform: rotateX(60deg) rotateY(360deg) translateZ(0); }
}

.ph2-display-core {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 100px;
  height: 100px;
  z-index: 4;
}

.ph2-core-orb {
  width: 100%;
  height: 100%;
  background: radial-gradient(circle, #00FFFF 0%, #0066FF 30%, #FF00FF 60%, #FF6600 100%);
  border-radius: 50%;
  animation: ph2-orbGlow 4s ease-in-out infinite alternate;
  box-shadow: 0 0 50px #00FFFF, inset 0 0 30px rgba(255, 255, 255, 0.3);
  will-change: transform, box-shadow;
}

.ph2-core-pulse {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 120%;
  height: 120%;
  border: 2px solid #00FFFF;
  border-radius: 50%;
  opacity: 0.6;
  animation: ph2-pulseExpand 3s ease-out infinite;
  will-change: transform, opacity;
}

@keyframes ph2-orbGlow {
  0% {
    box-shadow: 0 0 50px #00FFFF, inset 0 0 30px rgba(255, 255, 255, 0.3);
    transform: scale(1);
  }
  100% {
    box-shadow: 0 0 80px #FF00FF, inset 0 0 50px rgba(255, 255, 255, 0.5);
    transform: scale(1.1);
  }
}

@keyframes ph2-pulseExpand {
  0% {
    transform: translate(-50%, -50%) scale(0.8);
    opacity: 0.8;
  }
  100% {
    transform: translate(-50%, -50%) scale(2);
    opacity: 0;
  }
}

/* Scroll indicator with bounce animation */
.ph2-scroll-indicator {
  position: absolute;
  bottom: 3rem;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
  color: #00FFFF;
  cursor: pointer;
  transition: opacity 0.4s ease, transform 0.4s ease;
  z-index: 3;
  will-change: opacity, transform;
}

.ph2-scroll-indicator:hover {
  color: #00FFFF;
  transform: translateX(-50%) scale(1.1);
}

.ph2-scroll-line {
  width: 2px;
  height: 50px;
  background: linear-gradient(to bottom, #00FFFF, transparent);
  position: relative;
  overflow: hidden;
}

.ph2-scroll-line::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 20px;
  background: linear-gradient(to bottom, transparent, #00FFFF);
  animation: ph2-scrollDot 2s ease-in-out infinite;
}

@keyframes ph2-scrollDot {
  0% { transform: translateY(-20px); opacity: 0; }
  50% { opacity: 1; }
  100% { transform: translateY(50px); opacity: 0; }
}

.ph2-animate-bounce {
  animation: ph2-bounce 3s infinite;
}

@keyframes ph2-bounce {
  0%, 20%, 50%, 80%, 100% { transform: translateX(-50%) translateY(0); }
  40% { transform: translateX(-50%) translateY(-15px); }
  60% { transform: translateX(-50%) translateY(-8px); }
}

/* Responsive adjustments */
@media (max-width: 1200px) {
  .ph2-hero {
    border-width: 1px;
  }

  .ph2-hero-content {
    grid-template-columns: 1fr;
    text-align: center;
    margin-top: 40px;
    gap: 3rem;
  }

  .ph2-logo-image {
    height: 4rem; /* Matches font-size: 4rem */
  }

  .ph2-hero-visual {
    height: 500px;
  }

  .ph2-central-display {
    width: 250px;
    height: 250px;
  }
}

@media (max-width: 768px) {
  .ph2-hero {
    border-width: 1px;
    box-shadow: 0 0 15px rgba(0, 255, 255, 0.4);
  }

  .ph2-logo-image {
    height: 3rem; /* Matches font-size: 3rem */
  }

  .ph2-hero-buttons {
    flex-direction: column;
    align-items: center;
  }

  .ph2-hero-stats {
    justify-content: center;
    gap: 1rem;
  }

  .ph2-stat-number {
    font-size: 2rem;
  }

  .ph2-floating-card {
    display: none;
  }

  .ph2-hero-visual {
    height: 350px;
  }

  .ph2-central-display {
    width: 200px;
    height: 200px;
  }
}

@media (max-width: 480px) {
  .ph2-hero {
    border-width: 1px;
    box-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
  }

  .ph2-hero-content {
    padding: 0 1rem;
    margin-top: 40px;
  }

  .ph2-logo-image {
    height: 2.5rem; /* Matches font-size: 2.5rem */
  }

  .ph2-hero-description {
    font-size: 1.1rem;
  }

  .ph2-cta-button {
    padding: 1rem 1.5rem;
    width: 100%;
    justify-content: center;
  }
}